# This workflow builds the project for multiple platforms 
# and creates a release when a new tag is pushed.

name: Build releases

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    name: Build ${{ matrix.goos }}/${{ matrix.goarch }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          # Windows builds
          - goos: windows
            goarch: amd64
            ext: .exe
          - goos: windows
            goarch: arm64
            ext: .exe

          # Linux builds
          - goos: linux
            goarch: amd64
            ext: ""
          - goos: linux
            goarch: arm64
            ext: ""

          # macOS builds
          - goos: darwin
            goarch: amd64
            ext: ""
          - goos: darwin
            goarch: arm64
            ext: ""

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.26"
          cache: true

      - name: Checkout private dependency mermaid-mcp
        uses: actions/checkout@v4
        with:
          repository: xdung24/mermaid-mcp
          path: mermaid-mcp
          token: ${{ secrets.PRV_REPOS_TOKEN }}

      - name: Replace dependency with local path mermaid-mcp
        run: go mod edit -replace=github.com/xdung24/mermaid-mcp=./mermaid-mcp
      
      - name: Checkout private dependency bpmn-mcp
        uses: actions/checkout@v4
        with:
          repository: xdung24/bpmn-mcp
          path: bpmn-mcp
          token: ${{ secrets.PRV_REPOS_TOKEN }}

      - name: Replace dependency with local path bpmn-mcp
        run: go mod edit -replace=github.com/xdung24/bpmn-mcp=./bpmn-mcp

      - name: Checkout private dependency drawio-mcp
        uses: actions/checkout@v4
        with:
          repository: xdung24/drawio-mcp
          path: drawio-mcp
          token: ${{ secrets.PRV_REPOS_TOKEN }}

      - name: Replace dependency with local path drawio-mcp
        run: go mod edit -replace=github.com/xdung24/drawio-mcp=./drawio-mcp

      - name: Download dependencies
        run: go mod download

      - name: Build binary
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: |
          mkdir -p dist
          BINARY="diagram-mcp-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.ext }}"
          VERSION="${GITHUB_REF_NAME#v}"
          if [ "${GITHUB_REF_TYPE}" != "tag" ]; then
            VERSION="dev"
          fi
          go build -trimpath -ldflags "-s -w -X main.version=${VERSION}" -o "dist/${BINARY}" cmd/diagram-mcp/main.go
          ls -la dist/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: diagram-mcp-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/*
          if-no-files-found: error

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./dist

      - name: Flatten artifacts
        run: |
          mkdir -p ./release
          find ./dist -type f -exec mv {} ./release/ \;
          ls -la ./release

      - name: Generate checksums
        run: |
          cd ./release
          sha256sum * > SHA256SUMS
          cat SHA256SUMS

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: ./release/*
          draft: false
          prerelease: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
